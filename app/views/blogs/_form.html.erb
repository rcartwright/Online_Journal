<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :blog_name %><br>
    <%= f.text_field :blog_name %>

  <%= f.fields_for :user do |q| %>
  <div class="field">
    <%= q.label :name %><br>
    <%= q.text_field :name %>
  </div>
  <div class="field">
    <%= q.label :email %><br>
    <%= q.text_field :email %>
  </div>
  <div class="field">
<%= q.label :password %><br>
<%= q.password_field :password %>
</div>
  <div class="field">
<%= q.label :password_confirmation, "Confirmation" %><br>
<%= q.password_field :password_confirmation %>
</div>
<% end %>

<div class="actions">
    <%= f.submit %>
  </div>

<% end %>
