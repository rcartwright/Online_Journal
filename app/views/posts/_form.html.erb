<%= bootstrap_form_for([@blog, @post]) do |f| %>
<%= tinymce %>
    <%= f.text_field :title %>
    <%= f.text_area :body, :rows => "15", :class => "tinymce" %>
    <div class="actions">
    <%= f.submit %>
</div>

<% end %>
