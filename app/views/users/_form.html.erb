<%= bootstrap_form_for([@blog, @user], layout: :horizontal) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :blog do |q| %>
    <%= q.label :blog_name %><br>
    <%= q.text_field :blog_name %>
<% end -%>

    <%= f.text_field :name %>

    <%= f.text_field :email %>

  <%= f.file_field :avatar %>

<%= f.password_field :password %>

<%= f.password_field :password_confirmation %>

<div class="actions">
    <%= f.submit %>
  </div>
<% end %>
